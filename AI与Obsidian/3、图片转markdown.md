图片转 markdown 的提示词
```markdown
请将这张图片完整转为 Markdown。

要求：
1. 只输出 Markdown，不要解释。
2. 保留原有标题、层级、列表、表格、引用、代码块等结构。
3. 图片中的文字按原意准确转写，不随意改写。
4. 如果有表格，用 Markdown 表格输出。
5. 如果有公式，使用 LaTeX 格式。
6. 如果有看不清的内容，用 [无法识别] 标注。
7. 不要补充图片中没有的信息。
```